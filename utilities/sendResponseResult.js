// const { response } = require("express");

// const sendResponseResult = (a, b, c, d, e, f) => {

//     if (a && b && c && d && e && f) {
//         
//     }

//     // res.redirect("/results")
// }

// module.exports = sendResponseResult;